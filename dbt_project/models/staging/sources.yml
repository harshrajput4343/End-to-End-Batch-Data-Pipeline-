version: 2

sources:
  - name: raw_data
    schema: raw_data
    tables:
      - name: events
        description: "Raw event logs ingested from CSV via S3 COPY"
        columns:
          - name: timestamp
            description: "The time the event occurred"
            tests:
              - not_null
